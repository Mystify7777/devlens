/** Default max number of events retained by an EventBus's replay buffer. */
export const DEFAULT_REPLAY_BUFFER_SIZE = 1000;

/** Default max number of events retained by an EventStore. */
export const DEFAULT_STORE_SIZE = 10000;