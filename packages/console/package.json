{
  "name": "@devlens/console",
  "version": "0.0.1",
  "description": "Intercepts console.log/info/debug/warn/error and reports them to the DevLens Event Bus, while preserving native console behavior.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@devlens/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}