{
  "name": "devlens",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "pnpm -r exec tsc --noEmit"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "typescript": "^5.5.4"
  }
}
